# Pool v1.2.5 / Platform v0.17.0 session-security migration evidence

## Immutable input and boundary

- Previous Pool release: `v1.2.4`.
- Platform pin: `v0.17.0` commit
  `3a526defd21d692292c73652966a044167f881d7`.
- Worker Core: `0.8.0`.

Pool consumes only bounded signed-token, cookie-serialization, and same-origin
request mechanics. Pool retains secret selection, nonce and session records,
login/session TTL selection, one-time consumption, roles, campaign scopes,
CSRF tokens and header names, routes, rate limits, storage, credentials, email,
deployment, and rollback.

## Characterization and hardening

- The existing replay, fixed-expiry, expired-session, CSRF, cross-origin, and
  no-durable-write failure tests were retained before extraction.
- A characterization-only commit locked the exact HTTPS cookie name, path,
  `HttpOnly`, `SameSite=Lax`, `Max-Age=28800`, and `Secure` shape.
- The consumer migration rejects extra token segments before consuming the
  one-time nonce. Platform additionally bounds token, payload, secret, cookie,
  and claim-policy input.

## Required release evidence and rollback

- The full pre-merge gate passed: 827 unit tests passed with one intentional
  skip, 110 browser tests passed with three intentional skips, and every
  secret, syntax, focused regression, release-command, build-artifact,
  security, Podman resource, host Worker, and mutable-pledge phase passed.
- Root and Worker production dependency audits reported zero vulnerabilities;
  the production Wrangler configuration bundled successfully in dry-run mode.
- A protected pull-request smoke remains required after Platform `v0.17.0`
  and preceding Pool releases are merged.
- Roll back one Pool commit and restore the v1.2.4 Platform pointer. Every
  other consumer retains its own exact pin and needs no rollback.
