# Pool v1.2.1 Platform v0.12.0 migration evidence

## Scope

- Previous Platform pin: `7c5d4e8b44b25b9a649920c5a56d120ca29fc06f`
  (`v0.11.5`).
- Reviewed Platform pin: `09cc3cf1a35950fd410c652de390d44e71dd6fee`
  (`v0.12.0`).
- Extracted only the Pool/Store exact duplicates for header navigation, live
  announcements, timezone discovery, the New Mexico GRT starter reference and
  updater, and generated CSS/JavaScript minification.

Pool continues to own all templates, labels, routes, styles, campaign and
pledge rules, scheduling boundaries, tax-provider and taxability policy,
configuration, credentials, storage, builds, and deployment.

## Migration verification

- The Platform release source was byte-for-byte compared with both consumer
  implementations before the local copies were removed.
- Existing Pool behavior tests now execute the pinned Platform source through
  the same consumer fixtures.
- `tests/unit/platform-pin.test.ts` locks the canonical remote, exact commit,
  exact workspace/package versions, and every consumed source path.
- Local `npm run test:premerge` passed every phase: 823 unit tests passed with
  one intentional skip; build, budgets, security, host Worker, Podman resource,
  and mutable-pledge smoke passed; 109 browser tests passed with three skips
  and one successful retry.
- The protected PR check remains required before merge.

## Independent rollback

Revert the single Pool migration commit and redeploy the prior reviewed Pool
release. That restores the prior Platform pin and every local duplicate in one
atomic consumer change; Store, Dust Wave, Podcast, and Platform remain
unchanged. Verify the restored pin with the focused Platform pin test before
deployment.
