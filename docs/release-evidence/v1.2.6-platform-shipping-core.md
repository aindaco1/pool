# Pool v1.2.6 / Platform v0.18.0 Shipping Core migration evidence

## Immutable input and boundary

- Previous Pool release: `v1.2.5`.
- Platform pin: `v0.18.0` commit
  `3b8bdacc224bda625103718ba0fa8489517ff993`.
- Shipping Core: `0.1.0`.

Pool consumes deterministic shipping profiles, mixed-shipment aggregation,
missing-metadata summaries, fallback/free/manual quote shapes, and option
selection. Pool retains campaign policy, destination validation, USPS provider
configuration, credentials, requests, OAuth, cache, backoff, retries, checkout,
fulfillment, storage, deployment, and rollback.

## Characterization and reduction

- An independent consumer commit captured a mixed physical tier,
  support-item, and add-on shipment plus selected-option fallback before code
  moved.
- Existing Pool shipping coverage exercises missing metadata, campaign and
  platform fallback, free shipping, manual flat eligibility, domestic and
  international USPS quotes, timeouts, 401 refresh, rate limits, server
  failures, cache expiry, cooldown, configured options, and credential absence.
- The consumer shipping module is reduced by 542 duplicate lines. Its remaining
  provider transport and campaign orchestration are Pool-owned.

## Required release evidence and rollback

- The full pre-merge gate passed: 829 unit tests passed with one intentional
  skip, 110 browser tests passed with three intentional skips, and every
  secret, syntax, focused regression, release-command, build-artifact,
  security, Podman resource, host Worker, and mutable-pledge phase passed.
- Root and Worker production dependency audits reported zero vulnerabilities;
  the production Wrangler configuration bundled successfully in dry-run mode.
- A protected pull-request smoke remains required after Platform `v0.18.0`
  and preceding Pool releases are merged.
- Roll back one Pool commit and restore the v1.2.5 Platform pointer; no other
  consumer rollback is required.
