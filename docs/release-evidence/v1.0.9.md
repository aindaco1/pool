# Pool v1.0.9 Release Evidence

Generated for branch `release/1.0.9` on 2026-07-12.

## Scope

This release ports the production-operations capabilities relevant to Pool from Store v1.0.6 and v1.0.7 while preserving Pool's campaign/pledge semantics. Store-only products, coupons, ticketing, R2 downloads, and download-abuse controls are intentionally excluded.

## Automated evidence

| Area | Evidence | Result |
| --- | --- | --- |
| Recovery classification | `npm run backup:inventory:audit` | Pass; approved four-hour RPO/RTO and 7/5/12/release retention |
| Recovery readiness | `npm run backup:readiness` | Pass with expected warnings until a current encrypted live snapshot/provider artifact exists |
| Synthetic restore | `npm run restore:rehearse` | Pass; authoritative, derived, quarantine, and Durable Object policies exercised |
| Recovery unit coverage | Pool backup/restore/retention/offsite/readiness/reconciliation/traffic tests | Pass |
| Admin read model | Bulk-read, watermark, dashboard, analytics, supporter, and Film summary tests | Pass |
| Admin security | Session review/revoke, audit search/CSV, Turnstile, and dashboard tests | Pass |
| Cloudflare admin policy | Managed-rule construction/reconciliation/public verification unit tests | Pass |
| Build | Production Jekyll build plus generated asset minification | Pass |
| Asset budgets | `npm run performance:budget` | Pass after admin CSS split; Workers Cache explicitly off |
| Localization workflow | `npm run localization:review` | Workflow ready; no professional review claimed |
| Production posture | Config/parser/unit evidence plus live Worker secret-name inventory and strict DNS provider artifact | Pass; live audit status `ok` with no secret values exported |
| Workflow security | Immutable action pins, least permissions, deploy split, and package-script tests | Pass |
| Complete pre-merge gate | Secret/syntax/focused/full unit/release command/build/security/Podman/Worker/E2E phases | Pass locally: 763 unit tests with 1 intentional skip, 127 security tests, and 106 browser tests with 3 conditional skips. Clean-checkout [Merge Smoke run 29206409081](https://github.com/aindaco1/pool/actions/runs/29206409081) also passed the complete gate in 4m39s. |

Measured minified generated assets at implementation time:

- JavaScript total: 1,022,281 bytes / 1,075,000-byte ceiling
- CSS total: 228,384 bytes / 240,000-byte ceiling
- public `main.css`: 154,208 bytes / 175,000-byte ceiling
- admin-only `admin.css`: 72,828 bytes / 90,000-byte ceiling
- `admin-dashboard.js`: 418,037 bytes / 440,000-byte ceiling
- `cart-provider.js`: 222,852 bytes / 235,000-byte ceiling

## Store-to-Pool disposition

| Store capability | Pool v1.0.9 disposition |
| --- | --- |
| Classified encrypted backup/restore | Adapted to `PLEDGES`, `VOTES`, `RATELIMIT`, Git, Stripe, and Pool DOs |
| Recovery rehearsals and protected drills | Weekly synthetic active; quarterly traffic preflight active; captured-data job opt-in behind protected environment |
| Off-account archive proof | Private R2 target configured; encrypted bootstrap upload/readback/decrypt and lock rejection passed; captured-value release snapshot still pending |
| Bulk KV admin reads/read model | Adapted to campaign pledge indexes with 100-key reads and deterministic watermarks |
| Workers Cache | Disabled; requires representative ≥40% p95 improvement evidence |
| Admin session review/revoke | Adapted with Pool roles and privacy-minimized metadata |
| Searchable audit/CSV | Adapted to campaign, pledge, content, inventory, and adapter events |
| Deferred Turnstile | Adapted after the existing-session probe |
| Admin response no-transform policy | Adapted to `/admin` and `/es/admin` on `pool.dustwave.xyz` |
| Performance budgets/CSS split | Adapted with measured Pool ceilings and admin-only CSS |
| Production posture/localization review | Adapted to Pool bindings, secrets, campaigns, pledge/manage flows, and Spanish sources |
| Workflow SHA pins/Dependabot | Adapted across all Pool workflows and both npm projects |
| Deployment separation | Routine Pages refresh separated from reviewed manual Worker/full deploy |
| Store products/coupons/tickets/R2 downloads | Excluded as not applicable |

## External gates before production rollout

Configuration status on 2026-07-12:

- `production-recovery` matches Store's required-reviewer and `main`-only policy; `production-observability` is also `main`-only.
- Pool-specific age identity/recipient secrets, the shared Cloudflare zone ID, the Pool Worker name/base URL, the 100-request traffic threshold, and `RECOVERY_DRILL_ENABLED=false` are configured.
- Pool now has a private Standard-class R2 archive in a Cloudflare account separate from production. `production-recovery` holds bucket-scoped Object Read & Write credentials and the bounded `s3://pool-recovery-archive/pool-recovery` URI; repository variables select region `auto` and the account's HTTPS S3 endpoint.
- The R2 `pool-recovery/` prefix has an enabled 400-day bucket lock, exceeding the 12-month minimum. An age-encrypted bootstrap object passed upload, list, byte-identical readback, decrypt/plaintext comparison, and deletion-rejection verification on 2026-07-12.
- Because GitHub cannot export Store's Stripe recovery secret, Pool has its own permanent `Pool recovery read-only` restricted live key. Only Payment Intents Read is enabled; a live list request returned 200 and a harmless write probe against a nonexistent PaymentIntent was denied with 403. The key is stored only in `production-recovery`, and the one-time local capture was removed.
- Store-parity production posture is now complete: Pool has fresh production-only admin-session, settlement, and broadcast secrets; the matching broadcast value is synchronized to GitHub Actions; and the Store-compatible USPS client ID/secret passed the live quote smoke before configuration.
- The live posture audit passed with status `ok` using the actual `pledge-worker` secret-name inventory and a strict DNS provider artifact. The provider checker now writes the sanitized JSON artifact consumed by posture/readiness workflows; unit coverage verifies that unrecognized fields cannot leak into it.
- Pool still needs independent age-identity escrow and the first encrypted captured-value release snapshot. The R2 bootstrap object proves archive connectivity and immutability but is not a production-state snapshot.
- Store's existing Cache Response Rule pattern was reused in the shared `dustwave.xyz` zone for Pool. The active `Pool admin no-transform and no-store` rule is restricted to `pool.dustwave.xyz` plus `/admin`, `/admin/*`, `/es/admin`, and `/es/admin/*`.
- Public verification passed on 2026-07-12 for both `/admin/` and `/es/admin/`: status 200 with `max-age=0, private, must-revalidate, no-store, no-transform`, no Cloudflare script injection, and no report-only CSP.
- Spanish source-hash automation remains enabled, but human Spanish review is not a release gate.

- Keep public English and Spanish admin-route verification in provider evidence and production deployment with:
  `npm run cloudflare:admin-response-rule -- --verify-public --require-current`.
- If automated rule reconciliation is later required, provision the documented dedicated Cache Rules Edit token and use:
  `npm run cloudflare:admin-response-rule -- --apply --require-current`.
- Run the manual `Release Provider Evidence` workflow and retain strict DNS plus admin response-policy evidence.
- Retain the first successful scheduled/manual `Production Posture` workflow artifact after the branch is published; the equivalent live audit already reports `ok` with zero action findings.
- Create and retain the first encrypted remote KV-value release snapshot, then copy it off-device/off-account and rerun backup readiness with current evidence.
- Keep `RECOVERY_DRILL_ENABLED` false until the age identity is independently escrowed, a captured-value archive/readback has passed, and the operator approves enabling the drill. The required Cloudflare, permanent Stripe read-only, age automation, and R2 credentials are configured.
- Dispatch `Deploy Production` with an immutable reviewed ref. Routine `Refresh Production Pages` must not deploy the Worker.

## Acceptance

- [x] Pool semantics preserved; Durable Object state is not imported.
- [x] Existing Film summary adapter work retained and covered.
- [x] No secret values or customer data stored in sanitized evidence.
- [x] Recovery policy matches the approved Store policy.
- [x] Workers Cache remains evidence-gated and disabled.
- [x] Private off-account R2 target, scoped credentials, 400-day immutable prefix, and encrypted bootstrap readback verified.
- [x] Permanent live Stripe recovery key restricted to PaymentIntents Read, stored in the protected environment, and read/write-boundary tested.
- [x] Live Cloudflare admin response rule applied and publicly verified for both locale routes.
- [x] Direct live production-posture audit passes against strict provider evidence and the deployed Worker secret-name inventory.
- [ ] First encrypted off-account release snapshot retained.
- [ ] Live production posture workflow passes.
- [ ] Manual production deployment approved and executed.
