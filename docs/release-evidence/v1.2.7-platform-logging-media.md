# Pool v1.2.7 / Platform v0.19.0 logging and media migration evidence

## Immutable input and boundary

- Previous Pool release: `v1.2.6`.
- Platform pin: `v0.19.0` commit
  `1bfbdd403fc9efafb8d261dd846cedb9d52ed444`.
- Worker Core: `0.9.0`; Media Core: `0.4.0`.

Pool consumes the policy-injected scoped-console factory plus bounded media
path, classification, derivative, manifest, and placement-budget mechanics.
Pool retains config parsing, whether and where logs are emitted, ordinary
object redaction policy, campaign/default scope and slug policy, content,
filesystem access, transforms, admin routes, storage, deployment, and rollback.

## Characterization and reduction

- Existing Worker logger tests captured default, disabled, severity-filtered,
  prefix, and structured-error behavior before migration.
- An independent consumer commit captured exact path, label, public URL,
  responsive-image, video-source, classification, and derivative behavior
  before code moved.
- The two local modules are reduced by 190 lines and now contain only Pool
  policy and Platform adapters.
- Traversal paths now fail closed, and Platform bounds logger labels/error
  fields, media paths, and known-path collections.

## Required release evidence and rollback

- The full pre-merge gate passed: 831 unit tests passed with one intentional
  skip; the browser gate completed with 109 direct passes, one recovered
  Playwright retry, and three intentional skips; and every secret, syntax,
  focused regression, release-command, build-artifact, security, Podman
  resource, host Worker, and mutable-pledge phase passed. The retried
  keyboard-only checkout assertion then passed three consecutive isolated
  Podman runs.
- Root and Worker production dependency audits reported zero vulnerabilities;
  the top-level production Wrangler configuration bundled successfully in
  dry-run mode. A protected pull-request smoke remains required before release.
- Roll back one Pool commit and restore the v1.2.6 Platform pointer; no other
  consumer rollback is required.
