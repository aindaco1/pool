# Pool v1.2.4 / Platform v0.16.0 Resend migration evidence

## Immutable input and boundary

- Previous Pool release: `v1.2.3`.
- Platform pin: immutable `v0.16.0` commit
  `d075c3e1a29134d3ba6e4631b76dc63212347d14`.
- Worker Core: `0.7.0`.

Pool consumes only bounded raw-body Resend/Svix signature verification, the
provider-error metadata shape, and pure retry disposition. Pool retains the
Resend fetch, provider payloads, templates, audiences, consent, idempotency
keys, durable outbox, attempt budget, retry scheduling, webhook route/event
parsing, delivery journal, suppression effects, credentials, and deployment.

Platform contains no Pool credential, recipient, campaign content, provider
payload, environment ID, storage binding, or deployment authority.

## Characterization and hardening

- A test-only commit first fixed the current multiple-`v1`, stale timestamp,
  malformed-secret, exact-body, 429 `Retry-After`, and permanent-bounce
  behavior before the implementation moved.
- The local adapter preserves Pool's valid and mismatch response shapes and
  maps malformed timestamps to the established outside-tolerance result.
- Platform additionally bounds event IDs, signatures, and configured secret
  input and rejects fractional timestamps before HMAC work.

## Required release evidence and rollback

- The full pre-merge gate passed: 827 unit tests passed with one intentional
  skip, 109 browser tests passed with three intentional skips, and all secret,
  syntax, focused regression, release-command, build-artifact, security,
  Podman resource, host Worker, and mutable-pledge smoke phases passed.
- Root and Worker production dependency audits reported zero vulnerabilities.
- The production Wrangler configuration bundled successfully in dry-run mode.
- A protected pull-request smoke remains required after Platform `v0.16.0` is
  tagged and the preceding Pool `v1.2.3` consumer release is merged.
- Roll back one Pool commit and restore the v1.2.3 Platform pointer. Store,
  Dust Wave, Podcast, and Platform retain their own pins and require no
  rollback.
