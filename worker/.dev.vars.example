# Local Worker defaults and machine-specific secrets.
# Copy this file to worker/.dev.vars, or run `npm run secrets:dev`.
# The dev helpers can generate required local signing secrets.
#
# Keep this file aligned with local development needs, but never put real
# secret values here. worker/.dev.vars is ignored by git and should stay local.

# Local routing and mode.
APP_MODE=test
SITE_BASE=http://127.0.0.1:4000
WORKER_BASE=http://127.0.0.1:8787
CANONICAL_SITE_BASE=http://127.0.0.1:4000
CANONICAL_WORKER_BASE=http://127.0.0.1:8787
CORS_ALLOWED_ORIGIN=http://127.0.0.1:4000

# Local admin access.
# Replace with the email address you want to use for local admin sign-in.
# Multiple local bootstrap super admins may be comma-separated.
ADMIN_BOOTSTRAP_EMAILS=admin@example.com
ADMIN_TEST_CAMPAIGNS=hand-relations,smoke-editable

# Admin auth, sessions, and challenge protection.
ADMIN_SECRET=
# Optional scoped admin endpoint secrets. When set, settlement or broadcast
# routes require these narrower values instead of ADMIN_SECRET.
ADMIN_SETTLEMENT_SECRET=
ADMIN_BROADCAST_SECRET=
ADMIN_SESSION_SECRET=
CHECKOUT_INTENT_SECRET=
MAGIC_LINK_SECRET=
TURNSTILE_SECRET_KEY=
ADMIN_TURNSTILE_REQUIRED=false
ADMIN_TURNSTILE_BYPASS=false

# Stripe checkout and webhooks.
# Prefer the *_TEST variables for local development. STRIPE_SECRET_KEY and
# STRIPE_WEBHOOK_SECRET remain supported fallback names.
STRIPE_SECRET_KEY=
STRIPE_SECRET_KEY_TEST=
STRIPE_PUBLISHABLE_KEY_TEST=
STRIPE_WEBHOOK_SECRET=
STRIPE_WEBHOOK_SECRET_TEST=

# Email delivery and Resend plan usage.
RESEND_API_KEY=
# Resend safe API probes may expose rate-limit headers without monthly sent
# usage. These optional overrides keep the admin Plan usage display explicit.
PLAN_USAGE_RESEND_PLAN=
RESEND_EMAILS_MONTHLY_LIMIT=
RESEND_EMAILS_DAILY_LIMIT=

# Dashboard publish, report helpers, and Cloudflare plan usage.
GITHUB_TOKEN=
# Local remote-report/export scripts can use a read-only Cloudflare user API
# token with Account / Workers KV Storage / Read scoped to this account.
# GitHub Actions deploys need a separate user API token created from the
# "Edit Cloudflare Workers" template and stored as a repository secret.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
# Use a read-only GraphQL Analytics token for admin Settings -> Plan usage,
# not the broader deploy token above. Add Billing Read if you want Workers
# plan auto-detection; otherwise set PLAN_USAGE_CLOUDFLARE_PLAN.
CLOUDFLARE_USAGE_API_TOKEN=
CLOUDFLARE_WORKER_SCRIPT_NAME=
PLAN_USAGE_CLOUDFLARE_PLAN=
PLAN_USAGE_WARNING_PERCENT=80
PLAN_USAGE_CRITICAL_PERCENT=95
# Optional Cloudflare metric limit overrides are also supported with names
# such as CLOUDFLARE_WORKERS_REQUESTS_MONTHLY_LIMIT and
# CLOUDFLARE_KV_WRITES_DAILY_LIMIT when provider defaults do not match.

# Shipping and tax provider credentials.
USPS_ENABLED=false
USPS_CLIENT_ID=
USPS_CLIENT_SECRET=
ZIP_TAX_API_KEY=
