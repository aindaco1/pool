FROM node:20-bookworm

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    ca-certificates \
    git \
    python3 \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

CMD ["bash", "/workspace/scripts/podman-worker-entrypoint.sh"]
